# Expo public envs are embedded at build time.
# Copy this file to `.env` and adjust per your setup.

# Base URL of the Smart AC backend API.
# Examples:
# - iOS Simulator (API on host):          http://localhost:3000
# - Android Emulator (maps to host):      http://10.0.2.2:3000
# - Physical device (same Wi‑Fi as host): http://<your-lan-ip>:3000
EXPO_PUBLIC_BACKEND_BASE_URL=http://localhost:3000

